[package]
name = "farcooler-store"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[features]
# Fixtures other crates' tests need that only this crate can build, such as a
# database file at an older schema. Enabled from dev-dependencies only, so no
# shipped binary carries it.
testing = []

[dependencies]
farcooler-core.workspace = true
farcooler-protocol.workspace = true
rusqlite.workspace = true
serde_json.workspace = true
uuid.workspace = true
thiserror.workspace = true
tracing.workspace = true
